<h4>Location: {{fullLocation}}</h4>

<h3>Rentals available in {{city.name}}:</h3>
<ul>
  {{#each city.rentals as |rental|}}
    <li>{{rental.owner}}'s {{rental.type}} with {{rental.bedrooms}} rooms
    </li>
  {{/each}}

</ul>

<button {{action 'delete' city}}>Delete</button>
